# R package installation

Run once before executing the pipeline.

install.packages(c(
"tidyverse",
"data.table",
"reshape2",
"mlbench",
"caret",
"randomForest",
"xgboost",
"pROC",
"MLmetrics",
"themis"
))
